'use client'
// import { useSocket } from "@/components/SocketProvider";

export default function SocketIO() {
    // const { socket } = useSocket();

    // 这里可以使用 socket 进行其他操作
    return (
        <>
            <h2>SocketIO Component </h2>
        </>
    );
}